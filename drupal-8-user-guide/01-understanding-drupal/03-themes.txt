WHAT IS A THEME?
-- A theme is a set of files that define the visual look and feel of your site.
-- The core software and modules that run on  your site determine which "content" (including HTML text and other data stored in the database, uploaded images, and any other asset files) is displayed on the pages of your site.
-- The "theme" determins the HTML markup and CSS styling that wraps the content.